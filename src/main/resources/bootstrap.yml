spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hermeswaldemarin/microservices.series.configuration.git
          username:
          password:

server:
  port: 8888

management.security.enabled: false